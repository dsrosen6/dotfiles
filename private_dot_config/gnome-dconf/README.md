# dconf dump

This is just a regular dump of my gnome settings into a readable dconf.

When changes are detected at `~/.config/dconf/user`, a dump is automatically created via watchman and put here/updated in chezmoi.
